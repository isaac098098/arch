backend = "glx";
use-damage = false;
shadow = true;
corner-radius = 0;
fading = true;
fade-in-step = 0.01;
fade-out-step = 0.01;
fade-delta = 1;

shadow-exclude = [
	"class_g = 'Rofi'",
	"window_type = 'dnd'", 			#drag items
	"window_type = 'menu'", 		#Nautilus
	"window_type = 'dock'"
]
